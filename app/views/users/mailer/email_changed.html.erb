<p><%= t "activate_mail.greeting", email: @email %>!</p>
<% if @resource.try(:unconfirmed_email?) %>
  <p><%= t("users.changed.email", email: @resource.unconfirmed_email) %>.</p>
<% else %>
  <p><%= t("users.changed.email", email: @resource.email) %>.</p>
<% end %>
