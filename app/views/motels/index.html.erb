<%= provide :title, t("hotel.title") %>
<div id="paginator">
  <%= paginate @motels, remote: true %>
</div>
<div class="content">
  <ul id="motels" class="motels">
    <%= render @motels %>
  </ul>
</div>
<div id="paginator1">
  <%= paginate @motels, remote: true %>
</div>
<%= link_to t("hotel.add_hotel"), new_motel_path, class: "btn btn-lg btn-primary" %>
