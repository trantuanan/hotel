<%= provide :title, t("room.title") %>
<div id="paginator">
  <%= paginate @rooms, remote: true %>
</div>
<div class="content">
  <ul id="rooms" class="rooms">
    <%= render @rooms %>
  </ul>
</div>
<div id="paginator1">
  <%= paginate @rooms, remote: true %>
</div>

<%= link_to t("room.add_room"), new_room_path, class: "btn btn-lg btn-primary" %>
